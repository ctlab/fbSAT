for i in `ls logs/*.gz`; do dir=`gzip -cd $i | grep -o -P "outDir = .*$" - | cut -d " " -f 3- -`; if [ -d "$dir" ]; then echo "$dir $i"; fi; done | sort -r | sort -r -s -u -k 1,1 -t " " > logs_position
